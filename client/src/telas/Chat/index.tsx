import { View } from "react-native";

const Chat = () => {
  return (
    <View
      style={{
        width: "100%",
        height: "100%",
        backgroundColor: "gray",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Chat
    </View>
  );
};

export default Chat;
